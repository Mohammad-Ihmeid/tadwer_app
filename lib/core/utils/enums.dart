enum SignInRequestState {
  normal,
  loading,
  success,
  error,
}

enum RequestState {
  loading,
  loaded,
  error,
}

enum BottomState {
  prePress,
  loading,
  error,
  success,
}

enum SaveState {
  normal,
  loading,
  error,
  success,
}

enum ChangeListState {
  status1,
  status2,
}
